- name: Generate provider yaml
  template:
    src: provider.j2.yml
    dest: /tmp/provider.yml

- name: Display generated yaml
  command: cat /tmp/provider.yml
  register: output

- debug:
    var: output.stdout_lines
